docker-compose for grafana blackbox probing
---

Docker image which provides Prometheus exporters included.
- https://github.com/prometheus/blackbox_exporter


1. exec `docker-compose up -d`
2. access `localhost:3000`
